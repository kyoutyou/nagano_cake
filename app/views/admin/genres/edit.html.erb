<h4>ジャンル編集</h4>
<%= form_with model: @genre,url: admin_genre_path(@genre),method: :patch do |f| %>
<div class="field"></div>
  <p>ジャンル名</p>
    <%= f.text_field :name %>

  <div class="field"></div>
    <%= f.submit '変更を保存' %>
<% end %>
